############################################################
# Check lexical analyzer
############################################################
# comment

gtest = 10;

function gtestfunc(a) {
    global gtest;
    gtest = a;
    print(gtest);
}

function gtestfunc(a) {
    global gtest;
    gtest = a;
    print(gtest);
}

